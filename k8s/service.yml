apiVersion: v1
kind: Service
metadata:
  name: k8s-webapp-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: k8s-webapp
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080